- name: Variable playbook example
  hosts: all
  vars:
    URL: "www.goolge.com"  
    VAR1: "Hitesh Mankar"
  tasks:
     - name: print message
       debug:
          msg: "welcome to {{ URL }}, My Name is{{ VAR1 }}"


